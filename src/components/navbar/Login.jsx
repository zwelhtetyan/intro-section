import React from 'react';
import { Button, LoginContainer } from './Login.style';

const Login = () => {
    return (
        <LoginContainer>
            <p>login</p>
            <Button>Register</Button>
        </LoginContainer>
    );
};

export default Login;
